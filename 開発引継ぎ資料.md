# AIクイズアプリ 開発引継ぎ資料（v5）

> 教材から AI が問題を自動生成する学習アプリ。**1つのHTMLファイル完結**。
> 対象ファイル：`ai_quiz_app_v5.html`（アプリ本体）
> 補助ファイル：`起動.bat`（クロスOS起動ランチャー）／`server.ps1`（Windows用ローカルサーバ）／`backup/`（旧版）／`LICENSE`（MIT）／`THIRD-PARTY-LICENSES.md`（同梱ライブラリの帰属）／`README.md`
> 行番号は編集でずれるため、本資料は **関数名** で場所を示します（`Ctrl+F` で検索）。
> **最新の状態は §1.11（利用者FB対応：並べ替えドラッグ・文字数カウンタ）＋§1.10（バグ監査・修正）＋§1.9（試験モード）＋§1.8（問題プール・データ管理ハブ・バックアップ・堅牢化）を最優先で参照**（§1〜§1.7 は履歴）。
> **公開済み**：GitHub `Masah-111/AIQuiz-app`（Public・MIT）／GitHub Pages で配信中 → `https://masah-111.github.io/AIQuiz-app/ai_quiz_app_v5.html`

---

## 0. これは何か

教材（画像 / PDF / 貼り付けテキスト）を読み込ませると、**Claude API** が問題を自動生成し、出題・採点・復習・成績管理まで行う学習アプリ。フレームワーク不使用の素のJS。1ファイルに HTML/CSS/JS を全部内蔵。

- 動作モード2種：
  - **standalone**（単体HTML）：利用者の Anthropic APIキーでブラウザから直接 API 呼び出し（画像・PDF対応）。
  - **widget**（Claudeアーティファクト内）：`window.claude.complete`（テキストのみ）。`IS_WIDGET` で分岐。
- 既定モデル **`claude-sonnet-5`**（`PREFERRED_MODEL`。2026-07-01 に sonnet-4-6 から移行、§1.7）。廃止時は現行へ自動追従（`resolveActiveModel`）。

---

## 1. v4 → v5 の主な変更（最重要・引継ぎの肝）

| 区分 | 内容 |
|---|---|
| 数式表示 | **KaTeX を導入し、JS/CSS/フォントをHTMLにインライン同梱**（完全オフラインで数式表示）。表示テキストは `$...$`/`$$...$$` の LaTeX、**回答値は素のUnicode**。`mathToHtml`/`setMath` で描画。 |
| 数式キーボード | タブ切替式（基本/関係・演算/ギリシャ/三角・関数/微積・解析/**指数**）。普段は隠れ、右下ボタンで呼び出し。`☐`スロット・選択範囲を包む・回答後自動クローズ・下部余白で隠れ防止。`MATH_TABS` 等。 |
| 採点の刷新 | `normalizeText`（下付き→数字、上付き→キャレット、波/角/全角括弧→丸括弧、全角・記号・空白吸収）＋ `exprCanon`/`mathEquiv`（積・和の順序、方程式の両辺）＋ `answerMatches`。 |
| AI同値判定 | 文字列照合NG時にAIへ「同値か」を一括問い合わせ（`aiCheckEquivalence`）。ON/OFFトグル（`aiquiz_aieq`）。 |
| 手動上書き | 不正解セルに「別解として正解にする」ボタン（`acceptOverride`→`recomputeQuestionScore`）。 |
| 厳密採点 | 語学向け。`isStrictQuestion`（自動=`q.strict`/常に厳密/常に通常、`aiquiz_strict`）。厳密時は大小文字・空白を区別、AI/数式同値はOFF。 |
| 穴埋め自己修復 | `$...$` の中の空欄 `___` の扱い。**本セッションで方式変更**（`normalizeFillMath` 廃止 → `fillQuestionTextWith`、行列は分割せず「箱＋下に入力」）。§1.5 参照。 |
| 図 | 白背景向けの濃色指示／`sanitizeSVG` で薄色を自動濃色化（`colorLuminance`）／AI作図に「参考・誤りがあり得ます」キャプション。 |
| ヒント漏れ防止 | プロンプトに「仕分け項目にカテゴリ名を入れない」「図に答えを写さない」等を明記。 |
| 苦手復習(SRS) | 間違えた問題をlocalStorageに蓄積しLeitner式で後日再出題（`aiquiz_srs`、`addWrongToSRS`/`updateSRSAfterReview`/`startSRSReview`/`checkSRS`）。 |
| 中断ボタン | クイズ中つねに表示、進行を保存して後で再開（`quitQuiz`）。 |
| 印刷 | 結果画面のみ。「問題のみ／問題＋解答」の2ボタン（`printQuestions(includeAnswers)`）。数式も描画。 |
| 規約 | 第1〜18条に拡充（利用資格・禁止事項・アップロード禁止情報・再配布・準拠法・可分性 等）。 |
| 見た目 | 白背景テーマへCSS変数を整理（枠線・入力欄・文字階層・緑アクセント統一）。 |
| アイコン | 緑「A」のSVG favicon＋ヘッダー「ホーム画面に追加」ボタン＋案内モーダル（`addToHomeScreen`）。 |
| 起動 | OS別3ファイル → **1つのポリグロット `起動.bat`**（Windowsバッチ＋bash同居）＋ `server.ps1`。 |

---

## 1.5 本セッションでの追加・修正（v5公開後の最新／引継ぎ最重要）

> v5公開後に追加・修正した内容。**上の §1 の一部（特に「穴埋め自己修復」）はここで方式変更されている**ので、矛盾時はこちらが正。

### 論理バグ修正
| 箇所 | 内容 |
|---|---|
| 並べ替え採点 | `submitOrderAnswer`/`renderOrderQuestion`：**重複する文言の項目**があると正しく並べても不正解になる不具合を、インデックス比較→**値（テキスト）比較**に修正。 |
| 選択肢 | `normalizeQuestions`：`correct` が選択肢数の範囲外だと正解が選べないため、範囲外は 0 に補正。 |
| 難易度ピップ | `showQuizQuestion`：難易度順モードは適応OFFで `currentDifficulty` が固定なので、その場合は各問の `q.difficulty` を表示。 |
| 画像形式 | `handleFiles`/`SUPPORTED_UPLOAD_TYPES`：Anthropic非対応のHEIC等をアップロード時に警告して除外（対応は PDF/JPEG/PNG/GIF/WebP）。UIのHEICタグも削除。 |

### 穴埋めの「数式の中の空欄」（行列など）対応【方式変更・重要】
- 旧 `normalizeFillMath`（数式を空欄位置で分割）は**行列 `\begin{bmatrix}` や `\frac` を壊す**ため**廃止**。
- 共通ヘルパー **`fillQuestionTextWith(q, fill)`** に統一（出題・振り返り `formatFillQuestionText`・印刷で共用）。数式の外/中を判定して置換するだけで、**数式は分割しない**。
- `renderFillQuestion`：**数式の外**の空欄は従来どおりインライン入力。**数式の中**の空欄は `\boxed{(n)}` プレースホルダで数式を丸ごと描画し、入力欄は番号付きで**下にまとめて**表示。
- 採点はDOM順ではなく **`dataset.idx`** で `q.blanks` と対応（順序ずれ防止）。

### 再開時のファイル復元（IndexedDB）
- アップロード上限を **3**（`MAX_FILES`）に統一。
- 生成成功時に元ファイルを **IndexedDB**（DB `aiquiz` / store `resume_files`）へ保存（`persistResumeFiles`）。`localStorage` では容量的にPDFを保持できないため。
- 「続きから再開」で読み戻し（`restoreResumeFiles`）→ 図の切り出し・出典表示が復活。**完了・破棄で削除**（`clearResumeFiles`、`clearQuizState` から呼ぶ）。ヘルパーは `idbOpen`/`idbRun`。
- プライバシー：中断を放置している間は教材がブラウザ内に残る旨を使い方モーダルに明記。

### 記述（text）UI
- `renderTextQuestion`：回答欄を入力量に応じて**自動リサイズ**（上限＝画面の70%）。
- 行列が絡む記述では**書き方ヒント**（`[1 2; 3 4]` 表記）を表示（`involvesMatrix` で判定）。

### 数式テンプレートビルダー（旧・行列ビルダーを一般化）
- 1つのモーダルでテンプレートを選んでスロットを埋め、**KaTeXライブプレビュー**を見ながら、カーソル位置に素のテキスト表記を挿入する入力補助。**採点は賢くならない**（文字列一致＋AI同値のまま）＝入力の手間を減らすだけ。
- テンプレート：**行列／積分／総和／総積／極限／分数／場合分け**（`TB_LIST`、fields型は `TB_FIELDS`、行列と場合分けは可変サイズ）。生成例：`[1 2; 3 4]`／`∫[0,1] x^2 dx`／`Σ[i=1,n] i^2`／`Π[k=1,n] k`／`lim(x→0) (sin x)/x`／`(a+b)/(2)`／`{ x (x>0のとき); -x (x<0のとき) }`。
- 関数：`openTemplateBuilder(initial?)`/`closeTemplateBuilder`/`selectTemplate`/`renderTemplate`/`buildTemplateNotation`/`buildTemplatePreviewTex`/`insertTemplate`。旧名 `openMatrixBuilder`/`closeMatrixBuilder` は後方互換エイリアスで残置。
- 数式キーボードのヘッダー「⊞ 数式テンプレート」と、行列系記述のヒントボタンから開ける。
- 開いている間はキーボードを**モーダル前面**（CSS `above-modal`）に出し、**フォーカス中のスロットへ記号入力**（θ・^ 等）できる。挿入先は `tbTarget`（元の回答欄）。

### その他の小修正
- `mathToHtml`：`$...$` の中身に日本語（ひらがな・カタカナ・漢字）があれば数式描画せず素表示（「価格は$5と$10」の誤レンダリング対策）。
- `clearFigureCaches`：不要になったオブジェクトURLを `revokeFileUrls` で解放（メモリリーク防止）。

### β機能（実験的・**既定オフ**）
- ヘッダー「β機能」→ モーダルで特徴説明＋トグル（`aiquiz_beta_draw` / `aiquiz_beta_graph`）。ONにすると「回答形式」に該当β形式が出現し、`buildSystemPrompt` にその型仕様が加わる。`loadBetaSettings`/`onBetaToggle`/`applyBetaToFormatOptions`。
- **`draw`（自由描画＋AI採点）**：`<canvas>`＋Pointer Eventsで手描き → 画像をVisionで採点（`gradeDrawAnswer`、standalone/APIキー時のみ）。画像を送れない環境は模範解答と見比べる**自己採点**（`showDrawSelfGrade`）。`renderDrawQuestion`/`submitDrawAnswer`/`drawGradeAvailable`。
- **`graph`（格子点プロット）**：SVGグリッドの整数格子点をクリックで打点 → **決定論で採点**（`graphCorrect`：points=順不同／polyline=順序あり・逆順可）。`renderGraphQuestion`/`drawGraph`/`svgClientToLattice`/`submitGraphAnswer`。AI不要・追加料金なし。
- どちらも `QUESTION_TYPES` に追加し、normalize/render/採点/結果/印刷に統合。**生成品質は粗く、draw採点の精度は目安**なので本格使用は非推奨（だからβ）。

---

## 1.6 本セッション（スマホ対応・配布準備）での追加・修正【最新・最重要】

> §1.5 のさらに後。スマホ実機（Android / Brave）で #1〜#6 を確認済み。矛盾時は本節が最優先。
> 方針：**機能は変えずCSS中心**。挙動変更が要る所だけ最小限のJSを足し、既存ロジック・採点は不変。

### スマホ対応
- **iOS自動ズーム防止**：`@media (max-width:540px)` で文字入力系（input/textarea/**select**＋`.fill-input`/`.text-answer`/`.table-input`/`.mb-cell`）を `font-size:16px !important`。16px未満だとフォーカス時にSafariが拡大するため。`#api-key` はインライン14pxを持つので `!important` 必須。selectもタップでズームするので対象。
- **ヘッダーのハンバーガーメニュー化**：モバイルではナビ（`.header-nav#header-nav`）を畳み `☰`（`#hamburger-btn`）で開閉。`toggleHeaderMenu`/`closeHeaderMenu`。項目タップ・外側タップで自動クローズ。PCは従来の横並び（`☰` は `display:none`）。
- **モーダルの背面スクロールロック**：全 `.modal-overlay` の `.show` を MutationObserver で監視し、開いている間 `body.modal-open{overflow:hidden}`（`syncBodyScrollLock`/`watchModalsForScrollLock`）。β機能モーダルだけスクロール容器が無く中身が見切れていたのを修正（中身 div に `flex:1;overflow-y:auto`）。連鎖防止に各スクロール領域へ `overscroll-behavior:contain`。
- **数式キーボード×OSキーボードの二重表示抑制**：数式キーボードを開いている間、回答欄（`[data-mathmanaged]`）の `inputmode='none'` でOS側ソフトキーボードを抑制（`setOSKeyboardSuppressed`、`registerMathInput`/`toggleMathKeyboard` で制御）。閉じれば解除して通常入力（数字・英字・日本語IME）に戻る。**タッチ端末のみ**開いた瞬間に `blur()` で既出のOSキーボードを消す（PCはカーソル維持のため blur しない）。※採点は送信ボタンのみで走り、Enter送信ハンドラは無いのでIME変換確定での誤採点は起きない。
- **数式キーボードに数字行を追加**：タブ非依存の常時表示行（`1234567890.`）。`.mk-numrow#mk-numrow`＋`renderNumRow`（`buildMathKeyboard` で一度描画）。キー生成は `createMathKey` に共通化。記号入力中に数字を打つためキーボードを閉じなくて済む。
- **数式キーボードの整理（実機フィードバック）**：(1)`下付き`タブを新設（`指数`タブの対。下付き数字→演算子→英字→ギリシャ。**Unicodeに字形が無い b・c・d・f・g・q・w・y・z 等は出せない**）。(2)`基本`タブを「構造→演算子→定数」に整列し、散らばっていた下付き数字を `下付き` タブへ移動。(3)**`normalizeText` が下付き英字・演算子・ギリシャも通常文字へ畳む**ようにし（`SUB_MAP`）、`aᵢⱼ`→"aij" 等が採点で一致するようにした（従来は下付き数字のみ）。(4)`.mk-head` の説明文「数式キーボード — タブで切替…」を削除。(5)β機能モーダルと数式テンプレートビルダー（`#tb-body`）の内側 padding を `6px 2px`→`8px 16px`（スマホで横が詰まっていたため・同じ修正）。**注意：一部の下付きグリフ（ギリシャ ᵦᵧᵨᵩᵪ・ⱼ 等）は端末フォント次第で □ 表示になり得る。**
- **学習履歴ダッシュボードの狭幅崩れ**：スコア推移バーが枠外へはみ出す→`.dash-bars{overflow-x:auto}`＋`.dash-bar-wrap{min-width:22px}` で枠内スクロール。トピック名が1文字に潰れる→`.topic-bar-track`/`.topic-pct` の固定幅を縮めて名前に領域を回す。
- **カスタムドロップダウン**：ネイティブ `<select>` のポップアップはアニメ不可なので、6つの設定 select を自前UI（`.cdd`）に置換し開閉をふわっとアニメ化。**本物の `<select>` はDOMに残して値の源泉**とし、選択時に `change` を発火→既存ロジック・採点は不変。`overflow:hidden` の祖先（詳細設定の `.guide-box`）に切られないよう `position:fixed` で配置＋下端なら上開き。キーボード操作・ARIA 対応。関数：`enhanceSelect`/`initCustomDropdowns`/`closeAllDropdowns`。

### バグ修正
- **AI作成図（figure_svg）が消える**：`viewBox` だけで `width`/`height` 属性が無いSVGがレイアウト上 **0×0 に潰れて**いた。`sanitizeSVG` で viewBox から寸法を補い、描画要素が無い空SVGは `null`（ボックス・キャプションごと非表示）。
- **出典PDFがスマホで表示できない**：`<iframe>` 内PDFはモバイルブラウザで描画不可。`openSourceModal` を **pdf.js で該当ページを画像化**して表示する方式に変更（`renderPdfPage` を再利用）。失敗時は「PDF全体を別画面で開く」リンク（`makeSourceOpenLink`）にフォールバック。画像出典は従来どおり。

### PWA / ホーム画面追加
- head に `theme-color`／Apple用メタ（`apple-mobile-web-app-*`、`apple-mobile-web-app-title`）／`apple-touch-icon`。実行時 `setupPwa()` がアイコンSVGを canvas で PNG 化（180/192/512、`rasterizeIcon`）し apple-touch-icon を PNG に差し替え＋**Blob で manifest を注入**（単一HTMLのまま）。`addToHomeScreen` は standalone 検出（`isStandalone`）→ `beforeinstallprompt`（ワンタップ）→ 手順モーダルの順。モーダルに端末別ヒント（`showInstallHint`）。
- **制約（重要）**：`file://`／`content://`（ファイルマネージャ起動）では**安定したURLが無い**ため、ホーム画面追加・自動インストールは効かない。実際に効かせるには **https でホスティング**（GitHub Pages 等）が必要。入れた manifest／メタは配信時に有効化される。
- **→ 解決済み**：GitHub Pages（https）で配信し、**公開URLでの手動「ホーム画面に追加」を実機（Android）で確認済み**（緑Aアイコン＋AIQuiz名＋standalone）。なお完全自動のワンタップ・インストールは Service Worker 無し＋manifestをBlob注入のため出ないことがある（手動追加は可）。ワンタップ自動／オフラインまで欲しければ静的 `manifest.webmanifest`＋Service Worker を足す（任意）。

### ライセンス・配布
- 公開ライセンスは **MIT**（`LICENSE`）。同梱している **KaTeX（MIT）とそのフォント（SIL OFL 1.1）** の帰属を、HTML内コメントと `THIRD-PARTY-LICENSES.md` に明記。**pdf.js（Apache 2.0）はCDN読み込みで非同梱**。
- **公開済み**：Public リポジトリ `Masah-111/AIQuiz-app`。`README.md` 追加。GitHub Pages で `https://masah-111.github.io/AIQuiz-app/ai_quiz_app_v5.html` を配信。**HTMLにAPIキーは含まれない**ことを確認済み（`sk-ant-` はプレースホルダ／説明文のみ。キーは localStorage `aiquiz_api_key`、保存JSONからも `delete s.apiKey` で除外）。

---

## 1.7 本セッション（生成アルゴリズム・コスト・UX・SRS土台）での追加・修正【最新・最重要】

> §1.6 のさらに後（2026-07-01）。生成の質・コスト・UXの詰めと、次機能（カレンダー）の土台づくり。矛盾時は本節が最優先。**§1.5 の `mathToHtml` の記述はここで方式変更**。
> 検証は `node --check` ＋ ユニットテスト中心。**実機の本番フロー通し確認は未実施の項目が多い**（末尾「要実機確認」）。

### モデル移行：Sonnet 4.6 → Sonnet 5
- `PREFERRED_MODEL` を **`claude-sonnet-5`** に変更（§0）。理由：構造化出力(json_schema)対応の資格＋**導入価格 $2/$10（2026-08-31まで）** で入力が $3→$2。同一47ページPDFで **¥47.3→¥30.9（−35%）**、入力トークンはほぼ不変（PDFはページ画像課金でトークナイザ非依存）＝新トークナイザの副作用なし。質は同等以上（実機確認済み）。**ロールバックは `PREFERRED_MODEL` を戻すだけ**。移行要件（adaptive thinking明示・サンプリングパラメータ無し・ストリーミング・max_tokens余裕）は元から充足。導入価格は8/31以降 $3/$15 に戻る。

### B：adaptive thinking ＋ SSEストリーミング化（standalone/APIのみ）
- `generateQuestionsAPI` に **`thinking:{type:'adaptive'}` ＋ `output_config:{effort:'low'}`** を追加。教材読解→出題計画→漏れ・ネタバレ・矛盾の自己チェック→JSON出力、と質の3軸（文脈忠実性・網羅・ネタバレ抑制）に効く。思考コストは誤差（5問で出力〜2000tok）。
- **非ストリーミング→SSEストリーミング**に変更（max_tokens引き上げで応答が伸び接続タイムアウトの恐れ）。新ヘルパー **`readAnthropicStream(resp)`**：`text_delta` だけ結合（`thinking_delta`/`signature_delta` は本文に含めない＝JSON抽出は従来と同結果）。チャンク境界跨ぎ・CRLF正規化対応、`resp.body` 非対応環境は一括JSONへフォールバック。
- `max_tokens` を `min(8000,…)` → **`min(24000, 1500+問数×700+8000)`**（思考トークン用の余白）。

### コスト実測ログ ＋ アプリ内トースト
- **`logGenerationCost(usage)`**：SSEの `message_start`（入力・キャッシュ）と `message_delta`（出力）から実トークンを拾い、モデル別料金で概算コストを算出。コンソール出力＋ `window.__lastUsage` 保存。Sonnet 5 は導入価格を日付判定（`< 2026-09-01` → $2/$10）。**実測則：コスト ≈ PDFページ数 × ~¥0.65（S5導入価格）。KBではなくページ数で決まる**（ページ画像課金）。
- **アプリ内トースト**（コンソールが見えないスマホ向け）：生成直後に下部中央へ「💴 生成コスト ¥…（実測）」。タップで明細（トークン内訳）展開、×で閉じ、12秒で自動消去。body直下 `#cost-toast`（`position:fixed`）＋ `showCostToast`/`toggleCostToastDetail`/`hideCostToast`。standalone/APIモードのみ・実データがある時のみ表示。

### 数式描画の判定を修正（§1.5 の方式変更）
- 旧：`$...$` の中身に日本語があれば無条件で素表示（通貨 `$5と$10` 対策）→ `\frac{性能_X}{性能_Y}` のような**正当な日本語混じり数式まで生ソース表示**になっていた（ユーザ発見のバグ）。
- 新：**LaTeX構造（`\`コマンド・`{}`・上下付き `_`/`^`）を含めば日本語混じりでも数式描画**し、構造を含まない日本語のみ素表示（通貨対策は維持）。`mathToHtml` 内 `looksMath` 判定。

### 生成前ファネルの短縮（UX）
- **生成ボタンをコア設定（出題数・難易度・回答形式）の直後へ昇格**。「追加指示」を「詳細設定」`<details>` の中へ、「採点の設定」を新しい折り畳み `<details class="guide-box">` へ格納（既存アニメで統一）。既定パスで見えるのは教材入力＋コア3項目＋生成ボタンだけ。
- **APIキー欄の自動折り畳み**：キー保存済み（読込時）or 接続テスト成功時（1.2秒後）に「✓ APIキー設定済み（変更）」の1行へ畳む。`collapseApiKey`/`expandApiKey`（`applyModeUI`・`testConnection` から呼ぶ）。全input要素のIDは温存＝設定収集・採点ロジックは不変。

### SRSトピック副インデックス ＋ 試験日クリップ（カレンダー機能の土台）
- **primaryキー `srsKey`（=`normalizeText(question)+type`、問題単位の同一性）は変えず**、トピックの副インデックスを追加。類似問題を再生成すると問題文が変わり別srsKeyになる（＝同じ論点が別トラックに分裂）問題への対処。
- `normalizeTopic(s)`（NFKC＋小文字＋空白・約物除去。書式ゆれは吸収するが**同義語（微分法/導関数）は畳まない**）。SRSアイテムに `topic`/`nt` を保存（`addWrongToSRS`）。**`srsByTopic()`** がトピック単位で集約（`{normTopic,topic,items,due,dueCount,minBox}`）＝カレンダーの「今日やるトピック」表示の消費API。古いデータは `srsItemNT()` が `q.topic` から都度算出（後方互換）。
- **試験ストア**（`aiquiz_exams`／`loadExams`/`saveExams`/`addExam(name,'YYYY-MM-DD')`／`nextExamMs()`）。**`clipDueToExam(due)`** を `updateSRSAfterReview` に配線し、復習の `due` が直近試験日を超えないようクリップ（「試験5日前なのに次回16日後」を防ぐ）。**設定UIはカレンダー機能側で作る想定**、試験0件ならクリップ無効（現状挙動と同じ）。クリップは**グローバル**（直近1試験で全トピック一律。科目別はカレンダー実装時の精緻化）。

### 生成後の無料・決定論チェック（追加API呼び出しなし。2パス検証は不採用）
- **重要**：「JSON整合」の大半は**既に `normalizeQuestions` が実装済み**（choice `correct` 範囲補正、fill の `___`数=blanks数、sort の answer長=items長、table の blank範囲、各型の非空フィルタ）。**再実装せず**隙間だけ足した。
- **`integrityReason(q)`**：sort の `answer` 各値が実在カテゴリを指すか（範囲チェック）。範囲外は `auditQuestions` で除外（既存 `leakReason` と合流）。
- **`quoteAudit(qs)`**：**テキスト教材がある時だけ** `source.quote` が `storedSettings.materialText` 内に実在するか照合（`normQuote` で正規化）。不一致は捏造の疑いだが言い換え・数式・書式差で誤検出するため**除外せず** `q.quoteVerified`（true/false/null）に記録＋警告し、回答後の出典表示に「⚠ 引用未確認」を出す。**PDF/画像教材は materialText が空なので照合不可（null）＝PDF主体の使い方ではほぼ発火しない**。

### 苦手ノートPDF
- **`printQuestions(includeAnswers, qsArg, titleArg)`** にサブセット引数追加（後方互換：引数なし＝グローバル `questions`）。新 **`printSRSNote()`** が `loadSRS()` の**全苦手問題**を複製し、解答・解説つき・タイトル「AIクイズ 苦手ノート」で印刷。
- SRSバナーに「苦手をPDF化」ボタン（`#srs-pdf-btn`）追加。`checkSRS` を**苦手が1問でもあればバナー表示**（従来は期限到来時のみ）へ変更し、期限0なら復習ボタンを隠す。図（figure_svg/figure_ref）は印刷に出ない（印刷機能全体の既存制約）。

### 要実機確認（本セッションで静的検証止まりの項目）
- コストトースト（本番usageで表示されるか）／苦手ノートPDFの実印刷／SRSトピック集約・試験日クリップの実挙動／quote照合（テキスト教材で妥当に出るか）。**新機能を積む前に一度通しで確認推奨**。

---

## 1.8 本セッション（問題プール＝再利用無料化・データ管理ハブ・バックアップ・堅牢化）【最新・最重要】

> §1.7 のさらに後（2026-07-02）。狙いは「大きめPDFは初回だけ課金し、以降はAPIを呼ばず無料でくり返し出題」。検証は `node --check` ＋ プレビューでの `fetch`/`poolPut` モックによる単体確認が中心で、**実機（実PDF＋実キー＋実レート制限＋実容量オーバー）は未確認**。矛盾時は本節が最優先。

### 大前提（率直）：ブラウザではPDFを「保存して次回は思考だけ無料生成」はできない
- Anthropic APIはステートレス。生成のたびPDF（＝ページ画像課金）を毎回入力に載せる必要があり、**「読み込み済みを再利用してAPIが思考だけ」は不可能**。真に無料にできるのは「一度作った問題を保存して再利用」だけ。
- そのため本機能は「初回に大量の問題プールを作って保存 → 以降はプールから出題（¥0）」という設計。

### 問題プール（コード地図）
- **しきい値**：PDF総ページ数が `POOL_PAGE_THRESHOLD`(=5) 超で提案。`POOL_TARGET`(=60)問を目標、`POOL_BATCH`(=15)問ずつ、`POOL_MAX_BATCHES`(=12)まで。
- **保存先**：IndexedDB を **v1→v2** に上げ、新ストア **`pools`** を追加（`idbOpen` は既存 `resume_files` と両方を保証作成／`idbRun(mode, fn, storeName?)` に store 引数追加・既定は従来ストアで後方互換）。CRUD＝`poolPut`/`poolGet`/`poolDelete`/`poolList`。
- **同一ファイル判定**：`currentFilesFingerprint()` = 各ファイルの `名前:サイズ` ＋ `#総ページ数`（`countUploadedPdfPages`→`getPdfDoc.numPages`）のハッシュ。プールのIDに使う。
- **入口**：`startQuiz` が `preGenerateGuard` の前に `await maybeHandlePoolFlow(storedSettings)` を呼び、扱えば return。`maybeHandlePoolFlow` は「同一ファイルの既存プールあり→無料出題を確認して `serveFromPool`」「無ければページ数超で作成を確認して `startPoolBuildAndQuiz`」。
- **バックグラウンド作成**：`startPoolBuildAndQuiz` は**最初のバッチだけ待って即出題**し、残りは `continuePoolBuild` を **await せず**裏で実行。進捗は上部トースト `#pool-progress`（`showPoolProgress`/`hidePoolProgress`/`poolDoneToast`）。バッチ生成 `poolGenerateOneBatch` は `generateQuestions(s, null, files, {cachePdf:true})` を呼び、focus をローテートし重複を `poolNormQ` で排除。**ファイルは作成開始時のスナップショット**を渡すので、作成中にアップロードを変えても影響しない（`generateQuestions`/`generateQuestionsAPI` に第3引数 `filesArg` を追加済み）。
- **プールレコード＝「整然とした保存フォルダ」**：`{id, name(判別しやすい既定名＝PDF名＋作成日・変更可), createdAt/updatedAt, fileMeta, files(図・出典復元用のBlob), settings, questions, order, cursor, target, status}`。
- **出題（無料）**：`poolTakeSubset` がシャッフルした `order`＋`cursor` で**毎回ちがう問題**を返し（一巡で混ぜ直し）、`serveFromPool`→`restorePoolFiles`（Blobから `uploadedFiles` を復元＝図・出典が出る）→`startQuizFromQuestions(..., {fromSaved:true, poolId})`。`fromSaved:true` なので再生成・類似ボタンは出ない（＝プール出題から誤って課金しない）。
- **UI**：アップロード画面に `#pool-banner`（現ファイルに一致するプールがあれば「▶ プールから出題（無料）」）と `<details id="pool-library">`（全プール一覧＝出題/名前変更/削除、`poolItemHtml` を共用）。`refreshPoolUI`(→`refreshPoolBanner`+`refreshPoolLibrary`) を `handleFiles`/`removeFile`/`showScreen('screen-upload')`/初期化に配線。ウィジェットモードは対象外（PDF送信不可）。

### データ管理ハブ ＋ 全体バックアップ（1ファイル・キー除外）
- **率直な前提**：localStorage/IndexedDB は**ブラウザ内部・オリジン別**で、OSのフォルダには出せない。よって「総括」は (1) アプリ内1画面のハブ、(2) 全データ1ファイル書き出し、で実現。**実フォルダ保存（File System Access）はユーザー要望で不採用**。
- **ハブ**：ヘッダー「データ管理」→ `#data-modal`（既存モーダル構造＝スクロールロックObserver対応）。`openDataModal`/`closeDataModal`/`refreshDataModal` が APIキー状態・問題セット書出/読込・保存PDF件数・問題プール一覧・学習データ件数・全体バックアップ・容量目安を表示。
- **バックアップ**：`exportBackup()` が1つのJSON `AIQuiz_バックアップ_YYYYMMDD.json` を書き出す。中身＝`local`（`BACKUP_LOCAL_KEYS`＝履歴/苦手/試験/各設定。**`aiquiz_api_key` は意図的に除外**）＋`pools`（各プールの `files` Blob を `blobToB64` で base64 同梱＝**復元後も図・出典が出る**）。`importBackupFile()` は確認後、各プールを `b64ToBlob`→File 復元して保存＋localを上書き（キーは触らない）→ `location.reload()` で全反映。
- 容量：`storageEstimate()` が `{text, ratio}` を返し、`refreshDataModal` が `storageFull || ratio>=0.9` で容量行を**赤字＋⚠**。

### 生成の堅牢化（レビュー指摘 #1〜#5 対応）
- **#1 プール作成時のPDFキャッシュ**：`generateQuestionsAPI(settings, similarTo, filesArg, opts)` に `opts.cachePdf`。ON時は**最後のファイルブロック**に `cache_control:{type:'ephemeral'}`（system境界と合わせ計2つ／可変の指示テキストはキャッシュ外）。連続バッチは5分TTL内なので2バッチ目以降の入力が約1/10 → **プール総コスト約35〜45%減**。単発生成には付けない（1回だと書込分で割高）。
- **#2 429/5xxリトライ**：`reqInit` を1回組み、指数バックオフ（2s/4s/8s・上限30s・`retry-after`優先・ジッタ・最大3回）でリトライ。4xxは即エラー。ストリーム開始前の応答のみ対象。
- **#3 二重生成ブロック**：`preGenerateGuard` が `poolBuildActive` 中は警告して false（作成の初回バッチはフラグを立てる前に通るのでブロックされない／無料の `serveFromPool` はガード外）。
- **#4 スタック＆競合**：起動時 `reconcileStalePools()`（`refreshPoolUI` の前に実行）が中断で `building` のまま残ったプールを `ready` に正常化。`poolSaveMerge()`（`continuePoolBuild` で使用）は保存前に最新の `cursor/order/name` を尊重しつつ増えた `questions` を採用し lost-update を軽減。
- **#5 容量オーバーの通知**：保存失敗を握りつぶさず、`isQuotaError`/`notifyStorageFull`（容量エラーのみ・セッション1回アラート＋ハブ容量行を赤）／`poolSaveGuarded`＝`poolPut(...).catch(notifyStorageFull)`。データ保存経路（作成・追記 `poolSaveMerge`・cursor保存・バックアップ復元）をこれ経由に。※`renamePool` と起動時の正常化はデータ損失にならないので静かなまま。
- **見送り（ユーザー判断）**：#6 大容量バックアップのOOM対策（分割/zip＝複雑・数個なら不要）、#7 意味的な重複排除、#8 max_tokens切れ検知。

### 本セッションのバグ修正
- **「0問」幽霊プール**：`startPoolBuildAndQuiz` が初回バッチ前に空プールを保存していたため、初回バッチ失敗やタブ閉じで空の `building` プールが残り、次回起動時に `reconcileStalePools` が `ready` に昇格させて一覧に「0問」が居座っていた。修正＝**初回バッチ前の保存を廃止**（問題ができてから保存）、`continuePoolBuild` の finally で **1問も無ければ `poolDelete`**、`reconcileStalePools` は **空プールを掃除**。プレビューで確認（空building→削除／部分building→ready維持／通常ready→不変／一覧に0問が出ない）。

### 要実機確認（§1.8 追加分）
- 実PDF＋実キーでのプール複数バッチ作成・実コスト（#1キャッシュの効き）・図/出典の再利用表示／バックアップ書き出し→別ブラウザ復元（大容量base64のOOM有無）／429リトライの実挙動／容量オーバー通知（意図的に埋めるのは困難＝通常発火しない）。**新機能を積む前に一度通し確認推奨**（§1.7 分と合わせて）。

---

## 1.9 本セッション（試験モード）【最新・最重要】

> §1.8 のさらに後（2026-07-02）。「生成された問題を全問まとめて表示し、制限時間つきで一括採点する」試験形式を追加。検証は `node --check` ＋ プレビューでの機能テスト（正解6/6・部分正解・時間切れ自動採点・中断）。**実機での本番フロー（実生成→試験→採点）は未確認**。矛盾時は本節が最優先。

### UI配置（ユーザー要望どおり）
- トップの設定グリッド（出題数の隣）の**「初期難易度」を「モード」セレクト（`#quiz-mode`：通常モード／試験モード）に差し替え**。
- 玉突きで出た**「初期難易度」（`#difficulty`・IDは不変）は「詳細設定」`<details>` の中へ移動**。あわせて**「制限時間（試験モード・分）」`#exam-minutes`（既定30）**も詳細設定に追加。
- どちらのセレクトも `initCustomDropdowns()`（全 `<select>` を強化）が新DOM位置で自動的にカスタムドロップダウン化する（確認済み）。

### 仕組み（コード地図）
- **共通入口 `enterQuiz()`**：`storedSettings.mode==='exam'` なら `startExam()`、そうでなければ従来の `showQuizQuestion()`＋`showScreen('screen-quiz')`。統計バーの表示切替もここで行う。`startQuiz`（初回生成）・`startQuizFromQuestions`（保存セット／プール出題／デモ）・`redoSameQuiz` の3経路を `enterQuiz()` に集約。**類似問題(`startSimilarQuiz`)・再挑戦(`retakeQuiz`)は復習用途なので通常モードのまま**（意図的）。
- **試験専用レンダラー**：既存の単一問題レンダラー（固定ID `sort-submit`／`order-list` 等・グローバル `answered`/`sortSelections` 依存）は流用不可なので、`#screen-exam` に**専用の軽量レンダラー**を新設。`renderExamSheet()`→問題ごとに `#exq-{i}` カード＋`renderExamInput()`（choice=ラジオ／fill=番号付き入力（`examFillText` で空欄を(1)(2)…表示）／table=空欄セルに入力／text=textarea／sort=項目ごとに `<select>`／order=▲▼で `examOrderState[i]` を並べ替え／draw・graph(β)=「自動採点対象外」注記）。
- **タイマー**：`startExamTimer(分)`→`examEndTime`、`updateExamTimer()` を1秒間隔。残り≤60秒で赤字、0で `submitExam(true)`（自動提出）。**`showScreen` が `screen-exam` 以外へ遷移する時に `clearInterval(examTimer)`**（別画面で自動採点が走るのを防止）。
- **一括採点 `submitExam(auto)`→`gradeExamQuestion(q,i)`**：DOMから回答を読み型別に真偽を返す（choice=index一致／fill=`answerMatches(値,[blanks[i],...accept[i]], isStrictQuestion(q))`／table=`tableBlankAccepts`＋`answerMatches`／text=`keywordGrade().verdict==='correct'`／sort=cat index一致／order=`examOrderState`の値比較）。真偽を `history` に詰め `correctCount/wrongCount` を集計し、**既存の `showResults()` をそのまま呼ぶ**（結果画面・履歴保存・SRS登録は真偽配列だけで動くため再利用）。`quizKind='exam'`（`KIND_LABEL` に「試験」「苦手復習」を追加）。
- **中断 `quitExam()`**：確認後タイマー停止＋アップロードへ（採点しない）。

### 図・数式キーボード・AI同値判定に対応（ユーザー要望で追加）
- **図（figure_ref切り出し→figure_svg）を各問カード直下に表示**：`renderExamFigure(q,i,host)`（`renderQuestionFigure` のコンテナ版。`renderFigureCrop`/`sanitizeSVG` を流用、出典は `openSourceModal(i)`）を `renderExamSheet` から非同期で呼ぶ。
- **数式キーボード対応**：穴埋め・表・記述の入力欄に `registerMathInput()` を適用し、`startExam()` で `setMathToolsVisible(true)`。`showScreen` の数式ツール非表示条件を `screen-quiz` に加え `screen-exam` も除外（＝試験中も表示）。回答欄フォーカスで `activeMathInput` 化→「∑」ボタンでキーボード→`insertMathToken` で挿入（既存機構をそのまま利用）。
- **AI同値判定**：`submitExam` を **async 化**し、`gradeExamQuestion` の穴埋め・表を `gradeCellsExam(q,cells,strict)` に委譲。`runCellGrading` と同じロジックで、文字列一致で外れた**非空セル**を（非厳密かつ `aiEquivAvailable()` の時のみ）`aiCheckEquivalence` に**問題単位で1回**問い合わせて同値なら正解にする。採点中は `setExamGrading(true)` で提出ボタンを「採点中…」に。
- **残る割り切り**：記述(text)は簡易キーワード採点（`keywordGrade`）だった → **§1.10 #10 で通常モードと同じAI採点(`gradeTextAnswer`)に変更**。draw/graph(β)は自動採点対象外（注記表示）。`#exam-note` を対応内容に合わせて更新済み。

### 要実機確認（§1.9）
- 実生成→試験モード開始→制限時間内に解答→採点→結果／履歴反映／SRS登録／時間切れ自動採点。特に多問数（30問）でのKaTeX描画とレイアウト、モバイルでの入力（16px・ズーム防止は既存CSSが効くか）。

---

## 1.10 本セッション（全コード監査→重大・中程度バグ修正）【最新・最重要】

> §1.9 のさらに後（2026-07-03）。全コード通読の監査で見つかった重大3件＋中程度5件を修正。検証は `node --check` ＋ プレビューでの機能テスト（試験ガード／中断→再開／SRS×試験のbox更新／draw除外の集計／XSSエスケープ／プール形式フィルタ／記述60点）。**実機の本番フロー（実生成→試験）は引き続き未確認**。矛盾時は本節が最優先。

### 重大（試験モード×既存インフラの噛み合わせ）
1. **試験中の画面遷移で試験消滅**：試験中にヘッダー「学習履歴」→ `showScreen` がタイマーを止め、試験画面へ戻る手段が無く回答・問題が全損だった。→ 新フラグ **`examActive`**（`startExam` でON、提出成功・`quitExam` でOFF）を導入し、**`openDashboard` が実施中は alert で拒否**。
2. **試験モードに自動保存が無く、教材ファイルがIndexedDBに残留**：試験は `saveQuizState` を呼ばず、タブ閉じ・中断で (a)有料生成した問題が全損、(b)`persistResumeFiles` 済みの教材に破棄手段が無い（「完了・破棄で削除」の方針違反）だった。→ **`startExam` で `saveQuizState()`**（再開バナー→やり直し／破棄で教材も削除）、**`resumeQuiz` は `enterQuiz()` 経由**に変更（mode='exam' のスナップショットは試験を最初から・タイマーもリセットでやり直し。回答はDOMにしか無いため続きからは不可能＝仕様）。`quitExam` は状態を残し（メッセージ更新）、**デモのみ `clearQuizState()`**。
3. **SRS復習×試験モードでboxが壊れる**：`startExam` が `quizKind='exam'` で上書き→ `recordSRSFromResult` が通常扱い＝正解しても昇格・卒業しない、だった。→ **`quizKind==='srs'` なら上書きしない**（履歴種別も「苦手復習」のまま）。SRS復習を試験モードで行っても box が正しく更新される。

### 中程度
4. **draw/graph(β)が試験で強制不正解＆SRS登録**→ **採点対象外＝`history[i]=null`** に変更。`showResults` の正答率は**boolean のみを母数**に（`gradedTotal`）、振り返りは「－ 採点対象外」タグ、`saveQuizToHistory` のトピック集計・`recordSRSFromResult`（`===false` のみ登録）・`updateSRSAfterReview`（`===true`/`===false` で分岐、null はboxを動かさない）すべて null を除外。
5. **`renderFileList` のファイル名XSS**：innerHTML に生埋めだった → `escapeHtml(file.name)`。
6. **プール出題が「回答形式」を無視**：`poolTakeSubset(pool, n, wantFormat)` に形式フィルタを追加（指定形式のみからランダム抽出。該当0問なら alert してミックスにフォールバック）。`serveFromPool` は画面の `answerFormat`／`difficulty` を優先するよう変更。ミックスは従来どおりローテーション。
7. **記述(text)の合格基準がモード間で不整合**（通常=60点、試験=verdict'correct'=80点）→ 試験も **`keywordGrade().score >= 60`** に統一。
8. **`submitExam` に例外処理が無い**（想定外エラーで「採点中…」のまま固まる）→ try/catch で包み、失敗時は `examSubmitted=false` に戻して**再提出可能**に。

### 軽微（同セッション後半ですべて修正済み）
1. **印刷の並べ替えシャッフル**：`sort(random-0.5)`（偏りあり）→ `shuffleArray`（Fisher-Yates）＋偶然正解順のまま印刷される事故のガード（`renderOrderQuestion` と同じ基準）。
2. **同名ファイル追加の無言スキップ**：`handleFiles` が除外理由アラートに「同じ名前のファイルが追加済み」を合流。
3. **`FORMAT_LABEL`** に `draw:'描画(β)'`／`graph:'グラフ(β)'` を追加（受験履歴での生キー表示を解消）。
4. **プール同一ファイル判定の衝突対策＋後方互換**：現行IDは各ファイル**先頭8KBの内容ハッシュ**も含める（`fileSampleHash`／`currentFilesFingerprintIds()`＝`{id, legacyId}`）。**既存プール（旧ID）は `poolGetForCurrentFiles()` が参照時に新IDへ自動移行**（put成功後に旧を削除・失敗時は旧IDのまま継続）。`maybeHandlePoolFlow`／`refreshPoolBanner` はこの関数経由に変更。`startPoolBuildAndQuiz` の新規作成は新IDを使用。
5. **フィンガープリントのメモ化**：`fpMemo`（ファイル構成キーで保持、`clearFigureCaches`＝ファイル変更時に破棄）。アップロード画面を開くたびのPDF再解析・再ハッシュを回避。
6. **SRSの容量対策**：`addWrongToSRS` は問題を**複製して保存**（ライブオブジェクト不変）し、**50KB超の `figure_svg` だけ**保存対象から外す（通常サイズの図は復習でも表示される）。`saveSRS` の保存失敗は `console.warn`＋容量エラーなら `notifyStorageFull`（黙殺しない）。
7. **`sanitizeSVG` の外部参照遮断**：`href`/`xlink:href` が `#`（内部参照）・`data:` 以外なら属性を除去（外部画像・外部リンクを読み込ませない。script/on*/javascript: 除去は従来どおり）。
8. **試験モードの数式キーボード表示基準を通常モードと統一**：`startExam` が fill/table/text を含む時だけ `setMathToolsVisible(true)`（choiceのみの試験では出さない）。
10. **試験モードの記述(text)採点をAI採点に統一**（別Claudeの指摘→ユーザー承認。思考トークンのコストは実測で軽微と確認済み）：`gradeExamQuestion` の `case 'text'` を `keywordGrade().score>=60` → **`(await gradeTextAnswer(q,val)).score>=60`** に変更。これで通常モードと採点方式が一致（「言い回しは違うが内容は合っている」答案が試験でも正解になる）。キー無し/失敗時は `gradeTextAnswer` 内部で `keywordGrade` に自動フォールバックするため無料利用は壊れない。ただし**記述問題ごとに毎回AI呼び出しが確定**（穴埋め・表の同値判定は失敗セルのみ・トグル制御ありなのと非対称）＝提出時に記述数ぶんの費用。`#exam-note` に「記述はAIが採点し…時間・費用がかかることがあります」を追記。プレビュー検証：AI採点モックで keyword 0点の答案が正解化(100%)／無キーで keyword フォールバック（要点2/3=正解・0/3=不正解）。
9. **プール出題等が残留した試験モードで不意打ち開始する問題（ユーザー実機報告）**：以前の生成で「モード＝試験」を選んだまま「▶ プールから出題（無料）」等を押すと、確認なしで30分の試験が始まっていた。→ `startQuizFromQuestions` がモードを読む際、**試験モードなら開始前に confirm**（OK=試験／キャンセル=通常モード。セレクト自体は変えない）。**「AIで問題を生成する」ボタン経由**（`maybeHandlePoolFlow` の既存プール出題・`startPoolBuildAndQuiz` の初回出題）は画面でモードを見て押しているので **`modeConfirmed:true` で確認をスキップ**（`serveFromPool` が `s.modeConfirmed` をパススルー）。対象入口＝プールバナー/一覧・保存セット読込・デモ・苦手復習。通常モード時はダイアログなし（従来どおり）。プレビューで6ケース検証（バナー×OK=試験／キャンセル=通常＆セレクト不変／通常=確認なし／生成経由=モード再確認なしで試験／デモ×キャンセル=通常／SRS復習×OK=試験かつquizKind='srs'維持）。

検証：`node --check` ＋ プレビュー（印刷30回シャッフルで全項目維持・正解順ゼロ／同名アラート／内容違い同名PDFが別ID・旧IDプールの自動移行と2回目直ヒット・メモ化ヒット／巨大SVGのみ除外＆ライブ不変／外部href除去・#/data:維持／choiceのみ試験でキーボード非表示）。コンソールエラーなし。実機の本番フロー確認は引き続き未実施。

---

## 1.11 本セッション（初回・実利用者フィードバック対応）【最新・最重要】

> §1.10 のさらに後（2026-07-03）。実利用者（知財検定3級を勉強中のテスター）から初のFB（`Downloads/AI問題集メーカー 利用の感想.docx`）。ポジティブ多数（UIの分かりやすさ・他選択肢まで説明する解説・記述設問の的確さ）。改善要望4点のうち、明確・低リスクな2点（③-a 並べ替えドラッグ／③-b 記述の文字数カウンタ）を実装。残り2点は §7 に記載。検証は `node --check` ＋ プレビューでの実 PointerEvent／input 検証。**採点ロジックは不変**。

### ③-b 記述の文字数カウンタ
- **`parseCharLimit(q)`**：問題文に「N文字以内」「N字以内」（全角数字も可）があれば上限を返す（無ければ0）。
- **`attachCharCounter(textarea, q, container)`**：回答欄下に `.char-counter` を付け、`現在字`（上限があれば `現在 / 上限字`）を表示。超過で `.over`（赤・太字）。カウントは `[...value].length`（コードポイント単位）。数式キーボード挿入も input を発火するので自動反映。
- 配線：通常モード `renderTextQuestion`（textarea 追加直後）＋試験モード `renderExamInput` の text ケース。CSS `.char-counter`/`.char-counter.over` を追加。

### ③-a 並べ替え（order）のドラッグ操作【ブロック全体を掴む・通常＋試験の両モード対応】
- **▲▼ボタンは残したまま**、**行ブロック全体を掴んでドラッグ**（当初は局所ハンドルだったが、ユーザー要望で行全体を掴む方式に変更）。`↕`（`.order-drag`）は「掴める」目印＝**装飾（`pointer-events:none`）**、pointerdown は行が受ける。
- **汎用関数 `startBlockDrag(e, opts)`**（旧 `onOrderDragStart` を一般化・通常/試験で共用）。opts＝`{pos, getRows(), getOrder()(生配列をin-placeで入替), rerender(), isLocked()}`。**隣接行の中点を越えたら1つ入れ替える方式**（短いリスト向けでズレない）。`pointermove`/`up`/`cancel` は **document に張る**ので、入れ替えのたびの再描画を跨いでも追従。`e.target.closest('button')`（▲▼）と非左クリックはドラッグ開始しない。`isLocked()` が真になれば移動中でも自己終了。
- **状態を更新するだけ**（通常＝`orderState`／試験＝`examOrderState[i]`）なので、両モードの値ベース採点は不変。
- CSS：`.order-row.draggable`/`.exam-order-row.draggable{cursor:grab; touch-action:none}`（タッチ時のスクロール誤爆抑止）、`.dragging{枠・影}`。回答/提出前のみ `draggable` を付与し、`submitOrderAnswer` は `draggable` を外す＝カーソル/スクロール復帰＋ハンドル淡色。
- **試験モードにも適用**：`renderExamOrderRows` の各行に `exam-order-row`＋`↕`＋`startBlockDrag`（`getRows`/`rerender` は `#exq-{i}-order` スコープ、`getOrder`＝`examOrderState[i]`、`isLocked`＝`examSubmitted`）。▲▼(`examMoveOrder`)も併存。
- 検証：通常＝テキスト部を掴んで先頭→末尾へ移動・番号振り直し・▲▼非干渉・ハンドル装飾化(`pointer-events:none`)・回答後 `cursor:grab`解除、試験＝ドラッグで表示と `examOrderState` 一致・正解順で正解/誤順で不正解・▲▼併存。`node --check`＋プレビューでコンソールエラーなし。

### ①②の根本原因対処（プロンプト修正）＋ 文字数カウンタ「N字程度」対応
> PDF版FBのスクショで診断確定：②「著作者の死後70年間まで が×」は**表穴埋め(table)**で、格納正解が「著作者の死後70年**（原則）**」だったため文字列一致で外れていた（記述AI採点自体は正常＝画像6が30点/部分点で機能）。①（画像3 Q5 選択肢Cだけ「（原則）」）と②は**同一原因＝AIが回答値に括弧書き注釈を付ける**。→ `buildSystemPrompt` を修正して発生源を断つ。
- **ルール(2)強化**：正解だけに「定型句・括弧書きの注釈（（原則）（例外）等）」を付けない＋全選択肢を同じ粒度・長さ・書式にそろえる、を明記。
- **新ルール「回答値は核心の答えだけ（注釈を付けない）」**：choice選択肢・fillのblanks/accept・table空欄セルの値に（原則）（一身専属権）等の注釈・限定句・根拠を付けない。補足は explanation へ（誤「著作者の死後70年（原則）」→正「著作者の死後70年」の例つき）。
- **新ルール「accept を厚くする」**：語尾・助詞・送り仮名・単位の違い（「70年」「死後70年」「70年間」、「〜まで」の有無、「不可」「できない」等）を accept に列挙。核心が合っていれば末尾表現差で不正解にしない方針。
- **自己点検に (3b) 回答値のクリーンさ**を追加（注釈が付いていたら核心だけに直し補足を explanation へ／accept 補完の確認）。
- **既存の安全網は不変**：AI同値判定（ON＋キー時）＋「別解として正解にする」手動ボタンは残る。※プロンプト依存＝ソフト改善で100%保証ではない。実PDFで数問生成して「（原則）」が値から消えているか要確認。
- **文字数カウンタ「N字程度」対応**：`parseCharLimit` は `{n, hard}`（以内=hard／程度=soft）を返すよう変更。`attachCharCounter` は 以内=`現在/上限字`＋超過赤、程度=`現在/目安字（目安）`＋赤なし、指定なし=`現在字`。彼女の設問は「80字程度」（画像6）なので目安表示が実使用にフィット。
- 検証：`parseCharLimit`（以内/程度/全角/なし）、`buildSystemPrompt` が壊れず新ルール4点＋必須フィールド行を含む（7550字）、カウンタUI（soft超過は赤なし・hard超過は赤・指定なし現在数）。コンソールエラーなし。**プロンプト変更の実生成確認は未実施**。

### 「問題プールから読み込む」ボタン追加（プール機能の発見性向上）
- ユーザー要望（テスターがプール機能に気づきにくい）。「保存した問題を読み込む」`<label>` の隣に **`#load-pool-btn`「問題プールから読み込む」** を追加（親 `<div>` を `display:flex; gap:10px; flex-wrap:wrap` に）。
- **`openPoolLibrary()`**：`refreshPoolLibrary()` で最新化 → プールがあれば `#pool-library`（既存の details 一覧）を `.open=true` にして `scrollIntoView`（そのまま各プールの「出題（無料）」から選べる）。プールが無ければ「◯ページ以上のPDFで作成できます」と案内アラート。
- 検証：ボタンが同一行に10px間隔で並ぶ／プール有り＝一覧が開く(open=true・display:block)／プール無し＝案内アラートで一覧は開かない。`node --check`＋プレビューでコンソールエラーなし。

### 実機確認の到達点（2026-07-03）
- ユーザーが実機で以下を確認済み・良好：**問題プールの複数バッチ実生成＆実コスト（PDFキャッシュの効き）／バックアップの別ブラウザ復元／§1.11のプロンプト変更の効き（回答値の「（原則）」等注釈が消える）**。§1.9 試験モード・§1.10/§1.11 の各修正も実利用で問題なしとの報告。
- **未確認は 429リトライの実発火のみ**（意図的発火が困難＝据え置き。実装は残す）。→ 実機面の積み残しは実質解消。次は §7 の機能拡張（本命＝カレンダー）フェーズ。

---

## 2. 画面の流れ

```
[アップロード] 教材・条件・APIキー・採点設定
   │ startQuiz()
   ▼
[ローディング] 生成中
   │ generateQuestions() → normalizeQuestions()
   ▼
[クイズ] 1問ずつ出題・回答・解説   showQuizQuestion()
   │ nextQuestion() / quitQuiz()（中断）
   ▼
[結果] 正答率・振り返り・印刷/保存/復習導線   showResults()
[ダッシュボード] 学習履歴   openDashboard()
```
モーダル：利用規約（`openNoticeModal`）・使い方（`openHelpModal`）・出典（`openSourceModal`）・AI判定の説明（`showAiEqInfo`）・ホーム画面追加（`addToHomeScreen`）。

---

## 3. コード地図（主な関数）

- **生成**：`startQuiz` → `generateQuestions` → `generateQuestionsAPI`/widget → `normalizeQuestions` → `auditQuestions`。プロンプトは `buildSystemPrompt`（JSON仕様・ルール）＋ `buildInstruction`（出題条件）。**SSEストリーミング** `readAnthropicStream`、**コスト実測** `logGenerationCost` ＋トースト `showCostToast`/`toggleCostToastDetail`/`hideCostToast`、**生成後の決定論チェック** `leakReason`/`integrityReason`/`quoteAudit`（§1.7）。
- **出題描画**：`showQuizQuestion`、`renderSort/Order/Fill/Table/Text/Draw/Graph`系。数式は `mathToHtml`/`setMath`、穴埋めの空欄処理は `fillQuestionTextWith`、行列入力は行列ビルダー（`openMatrixBuilder`）。
- **採点**：`answerMatches`（`normalizeText`/`exprCanon`/`mathEquiv`）、`isStrictQuestion`、`runCellGrading`（`aiCheckEquivalence` フォールバック）、`acceptOverride`/`recomputeQuestionScore`、`finishAnswer`、記述は `gradeTextAnswer`/`keywordGrade`、β描画は `gradeDrawAnswer`（Vision）、βグラフは `graphCorrect`（決定論）。
- **数式テンプレートビルダー(共通UI)**：`openTemplateBuilder`/`renderTemplate`/`buildTemplateNotation`/`insertTemplate`（行列/積分/総和/総積/極限/分数/場合分け）。**β機能**：`loadBetaSettings`/`onBetaToggle`、draw=`renderDrawQuestion`/`submitDrawAnswer`、graph=`renderGraphQuestion`/`drawGraph`/`submitGraphAnswer`。
- **再開ファイル(IndexedDB)**：`persistResumeFiles`/`restoreResumeFiles`/`clearResumeFiles`/`idbOpen`/`idbRun`。
- **数式キーボード**：`MATH_TABS`、`buildMathKeyboard`/`switchMathTab`/`renderMathGrid`、数字行 `renderNumRow`(`MATH_NUM_KEYS`)、キー生成共通化 `createMathKey`、`insertMathToken`/`mathBackspace`、`registerMathInput`、`toggleMathKeyboard`/`updateKbPadding`/`setMathToolsVisible`、OSキーボード抑制 `setOSKeyboardSuppressed`。
- **図・出典**：`renderQuestionFigure`、`renderFigureCrop`、`sanitizeSVG`/`colorLuminance`、pdf.js遅延読込（`renderPdfPage`）。出典モーダルはPDFを **pdf.jsで画像化**して表示（`openSourceModal`）＋外部で開く `makeSourceOpenLink`。
- **モバイルUI**：ヘッダー `toggleHeaderMenu`/`closeHeaderMenu`、モーダル背面ロック `syncBodyScrollLock`/`watchModalsForScrollLock`。
- **カスタムドロップダウン**：`enhanceSelect`/`initCustomDropdowns`/`closeAllDropdowns`（ネイティブselectを `.cdd` に置換、値は本物selectへ`change`同期）。
- **PWA / ホーム画面追加**：`setupPwa`/`rasterizeIcon`/`isStandalone`/`showInstallHint`、`addToHomeScreen`/`closeInstallModal`。
- **SRS**：`addWrongToSRS`/`updateSRSAfterReview`/`srsDueItems`/`checkSRS`/`startSRSReview`/`recordSRSFromResult`。**トピック集約・試験日クリップ（§1.7・カレンダー土台）**：`normalizeTopic`/`srsByTopic`/`srsItemNT`、試験ストア `loadExams`/`saveExams`/`addExam`/`nextExamMs`/`clipDueToExam`。苦手ノート印刷 `printSRSNote`。
- **設定**：`loadAiEqSetting`/`onAiEqToggle`/`aiEquivAvailable`、`loadStrictSetting`/`onStrictModeChange`/`isStrictQuestion`。
- **履歴・再開**：`saveQuizToHistory`/`openDashboard`、`saveQuizState`/`loadQuizState`/`checkResume`/`resumeQuiz`/`quitQuiz`。
- **保存/読込/印刷/アイコン**：`exportQuestions`/`startQuizFromQuestions`、`printQuestions(includeAnswers, qsArg?, titleArg?)`（サブセット対応・§1.7）／苦手ノート `printSRSNote`、`addToHomeScreen`/`closeInstallModal`。

---

## 4. 出題データの“契約”（AIが返すJSON / 保存セットの形）

共通：`type, question, explanation, difficulty(easy|medium|hard), topic, source{file,page,section,quote}`、任意 `figure_svg`（SVG文字列）/ `figure_ref{file,page,bbox[0..1]}`。

| type | 必須/特有フィールド |
|---|---|
| `choice` | `choices[]`, `correct`(索引) |
| `sort` | `items[]`, `categories[]`, `answer[]`(各itemの正解カテゴリ索引) |
| `order` | `items[]`（**正解順**で格納、表示時にシャッフル） |
| `fill` | `question`内に `___`、`blanks[]`、任意 `accept[][]`、任意 `strict`(真偽) |
| `table` | `rows[][]`(全セル埋めた状態)、任意 `headers[]`、`blanks[{r,c,accept?}]`、任意 `strict` |
| `text` | `model_answer`, `keywords[]` |
| `draw`(β) | `model_answer`(描くべき図の説明＝採点基準), `keywords[]`(採点要点)。Visionで採点。βがONの時のみ生成。 |
| `graph`(β) | `grid{xmin,xmax,ymin,ymax}`(整数), `points[[x,y]]`(正解の格子点), `mode`("points"=順不同/"polyline"=順序あり)。決定論採点。βがONの時のみ生成。 |

ルール要点：**表示テキスト**（問題文・選択肢・解説・表セル等）は `$...$` の LaTeX。**回答値**（fillの`blanks`/`accept`、tableの空欄セル）は `$` を使わず素のUnicode。語学で綴り・大小文字が重要な問題は `strict:true`。

---

## 5. localStorage キー

`aiquiz_api_key`（APIキー）/ `aiquiz_history`（成績履歴）/ 中断データ `aiquiz_resume`（`RESUME_KEY`）/ `aiquiz_srs`（苦手復習ボックス）/ `aiquiz_aieq`（AI判定ON/OFF）/ `aiquiz_strict`（採点の厳密さ）/ `aiquiz_beta_draw`・`aiquiz_beta_graph`（β機能ON/OFF）/ `aiquiz_exams`（試験の科目名・日付。§1.7・カレンダー土台）/ `aiquiz_agreed`（規約同意）。**いずれもブラウザ単位**（端末間で共有されない）。

加えて、**IndexedDB**（DB `aiquiz`。§1.8 で **v1→v2**）には2つのストア：`resume_files`（再開用の元ファイル・完了/破棄で削除）と `pools`（§1.8 の問題プール。プールごとに問題＋元ファイルBlob＋設定を保持。ユーザーがデータ管理ハブ／一覧から削除）。localStorage は容量的にPDFを保持できないためBlobはIndexedDB。

なお **全体バックアップ**（§1.8・`exportBackup`）は上記のうち **`aiquiz_api_key` を除く localStorage ＋ `pools`（PDFはbase64同梱）** を1つのJSONにまとめる。復元は `importBackupFile`（キーは上書きしない）。

---

## 6. 既知の制約（バグではなく設計上の割り切り）

- **ローカルファイル（file://）ではAPIが遮断されることがある** → `起動.bat`/`server.ps1` で localhost 経由起動。
- **ホーム画面追加・PWAインストールは `file://`／`content://` では不可**（安定したURLが無いため、ブラウザがショートカットを作れない）。実際に効かせるには **https でホスティング**（GitHub Pages 等）が必要。head のメタ・実行時注入の manifest は配信時に有効化される。§1.6 参照。
- **AI任せのソフトな歯止め**（ヒント漏れ防止・図に答えを写さない・LaTeX/Unicode分離・`strict`自動付与）はプロンプト依存で**100%保証ではない**。数回の生成で要観察。
- **図の内容の正しさは自動検証不能**（「参考・誤りがあり得ます」と明示）。
- **数式同値**は `+ - ×`（積・和）の順序入替のみ。割り算・括弧・累乗を含む式は**完全一致のみ**（ただし上付き→`^`、波/角括弧→丸括弧、`x²`=`x^2` 等の正規化で多くは吸収）。上付きは `^n`、下付きは素の数字に畳む（`x²`と`x₂`は別物）。
- **クロスOSの“ダブルクリック起動”は1ファイルでは不可**（OSごとに拡張子/実行権限の仕組みが違う）。`起動.bat`はWindowsはダブルクリック、Mac/Linuxはターミナル実行 or `.command`にリネーム。
- **記述・β描画のAI採点は「目安」**。特に手描き図（β `draw`）の精密な読み取りはVisionが苦手で誤判定しやすい。β `graph` は決定論なので正確だが、AIが格子点で表せない問題を作ると解けないことがある（β＝生成品質は要観察）。
- **数式中の日本語**：通貨 `$5と$10` の誤レンダリング回避のため、`$...$` の中身が**LaTeX構造（`\`・`{}`・上下付き）を含まない日本語のみ**なら素表示（§1.7で判定を精緻化。`\frac{性能_X}…` 等、構造を含む式は日本語混じりでも描画する）。
- **quote照合はテキスト教材専用**：`source.quote` の教材内実在チェック（§1.7）は貼り付けテキストがある時だけ。PDF/画像教材は本文テキストが無いので照合できず「⚠ 引用未確認」は出ない。
- **試験日クリップはグローバル**：SRS復習の試験日クリップ（§1.7）は直近1試験で全トピック一律。複数科目を並行する場合の科目別クリップはカレンダー実装時の課題。
- **再開時の図・出典は IndexedDB が使える環境前提**。`file://` 直開きでは一部ブラウザで不安定なので、`起動.bat`（localhost）経由推奨。

---

## 7. 残作業

> **PC・スマホ・ホスティング・ホーム画面追加（手動）まで実機確認済み。さらに §1.7 で生成アルゴリズム・コスト・UX・質チェック・SRS土台を追加。** 下記は今後の候補（優先度順）。コア機能に欠けは無く、以降は機能拡張。

- **【2026-07-03 実機確認 概ね完了】** §1.7＋§1.8＋§1.11 の主要フローをユーザーが実機で通し確認：**問題プールの実PDF＋実キーでの複数バッチ作成・実コスト（PDFキャッシュの効き）・バックアップの別ブラウザ復元・§1.11のプロンプト変更の効き（回答値から「（原則）」等の注釈が消えることを確認）＝いずれも良好**。**429リトライの実発火のみ未確認**だが、意図的に混雑を起こせない性質のため**これ以上追わない（実装済みで据え置き）**とユーザー判断。→ 実機面の積み残しは実質解消。以降は機能拡張フェーズ。
- **本命：カレンダー機能（AIが試験日＋苦手分野から自動スケジュール提案）**：**土台は §1.7 で導入済み**（`addExam`／`srsByTopic`／`clipDueToExam`）。残りは「試験登録UI」「今日やるトピック表示（`srsByTopic()` を消費）」「試験日からの逆算スケジュール」。着手時に決める設計判断：グローバル→科目別クリップ、トピックの同義語畳み込み。
- **任意・小粒**：試験本番シミュレーション（解答時間の記録＋時間配分分析。履歴に時間は未記録）／プロンプトで「quoteは原文を一字一句」を強め PDF教材でも出典忠実性を上げる。
- **任意：構造化出力(json_schema)**：Sonnet 5 で対応可能に。JSONの形は保証できるが配列長・個数（4択・items数）は縛れず、parse失敗は現状ほぼ無い（`normalizeQuestions` が堅い）ので優先度低。
- **任意：PDFページ削減でコスト減**：コストはページ数比例。投入前に必要な章だけ手で抜けば済むため、自動化（ジャンクページ除去・ページ選択UI）は保留。
- **任意：ワンタップ自動インストール／オフライン対応**：現状は手動「ホーム画面に追加」のみ確認済み。完全自動のインストールプロンプトやオフライン動作が欲しければ、静的 `manifest.webmanifest` ＋ Service Worker を追加（ホスティング済みなので実現可能）。
- **任意：数式テンプレートビルダーの二重キーボード**：ビルダーのモーダル内スロットは `data-mathmanaged` 対象外なので、そこでは数式キーボードとOSキーボードの二重表示が残りうる（細かいエッジケース）。
- **任意：リポジトリ仕上げ**：README/About欄/トピック（topics）の充実、コミットメッセージの整備など。
- **β機能の作り込み**：`draw`/`graph` は「動く」段階で「こなれている」段階ではない。生成プロンプトの精度、draw採点の安定性、graphの問題妥当性（格子点で表せるか）は実使用での観察・調整が必要。本格運用は見送り中（既定オフ）。Pointer Events採用なのでスマホ・スタイラスとの相性は要観察。
- **微細なバグ**は今後の実使用で炙り出す段階。機能・ドキュメントとも現状で完了。

---

## 8. 配布のしかた

**ライセンスは MIT**（`LICENSE`）。同梱ライブラリの帰属は `THIRD-PARTY-LICENSES.md` と HTML 内コメントに記載（KaTeX=MIT・そのフォント=SIL OFL 1.1、pdf.js=Apache 2.0 はCDN読み込みで非同梱）。

配布方法は2通り：

1. **ファイルを手渡し**：`ai_quiz_app_v5.html` ＋ `起動.bat` ＋ `server.ps1`（＋ `LICENSE`/`THIRD-PARTY-LICENSES.md`/`README.md`）を同じフォルダで渡す。受け取った人は自分のAPIキーを入れて生成、または**保存済み問題セット（JSON）をキー不要で再生**できる。この方式では**ホーム画面アプリ化は不可**（§6/§7）。
2. **https でホスティング（推奨・実施済み）**：現在 **GitHub Pages で配信中** → `https://masah-111.github.io/AIQuiz-app/ai_quiz_app_v5.html`。リンクを渡すだけで配れ、**手動「ホーム画面に追加」が実機で動作することを確認済み**。更新はリポジトリへ push するだけで反映。

配布前、HTML自体にAPIキーは入らない（localStorage保存）が、念のためキーや個人データが残っていないか確認推奨（規約 第13条）。**公開リポジトリにしてもキーは含まれない**ので秘密漏洩はないが、同様に最終確認を。
